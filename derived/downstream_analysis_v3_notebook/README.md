# Status of these notebook-generated outputs

This directory contains the **corrected reproduction outputs** generated by the repository notebook and is expected to agree with `derived/downstream_analysis_v3/`, which is the authoritative script output.

The original user-supplied exploratory results are preserved unchanged in `runs/mimetic_analysis_v3_user_results.zip` and `source_artifacts/mimetic_analysis_v3.zip`. Their energy tables used an unnormalised Euclidean symmetric part; in particular, the order-eight value near `844.9` is not an energy-norm result because the stored weight is indefinite. Those exploratory energy-growth quantities are **superseded and must not be cited**.

For reported results, use only:

- `derived/downstream_analysis_v3/headline_downstream_metrics.csv`;
- `derived/downstream_analysis_v3/headline_energy_transient_history.csv`;
- `derived/downstream_analysis_v3/all_operator_energy_timestep_solver.csv`; and
- `docs/DOWNSTREAM_CONSEQUENCE_ANALYSIS.md`.

The notebook-generated novelty JSON files also retain the earlier `Castillo-Grone`/`Corbino-Castillo outstanding` vocabulary. That attribution is superseded by `derived/downstream_analysis_v3/partial_novelty_gate.json` and `derived/reference_attribution_audit/`: the loaded MOLE arrays are Corbino--Castillo and that item is already compared.
