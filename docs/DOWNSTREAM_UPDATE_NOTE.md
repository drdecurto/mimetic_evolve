# the current release downstream update

The concise release remains eleven pages and retains all four inline figures. The update:

- normalizes the capitalization of institutional affiliations;
- preserves the sign-test saturation caveat;
- adds the corrected semidiscrete $Q$-energy consequence, the normalized weighted-symmetry contrast (`1.23e-16` versus `0.522`), and the unchanged RK4 limit;
- retains the incomplete novelty screen as a limitation rather than a novelty claim;
- adds the immutable user-supplied analysis bundle, a corrected analysis script, an executable notebook, derived tables, and the energy-transient figure;
- does not claim faster iterative solution, because iteration counts depend on the solver, preconditioner, right-hand side, and formulation.

The main-text energy result is norm-specific: transient growth is shown under the order-six reference's native positive quadrature and does not exclude another Lyapunov norm.

The reference transient maximum is attained inside the tested interval at `t/h^2 = 0.333` and decays thereafter. The original user-notebook Euclidean energy-growth tables are retained only for provenance and are marked as superseded.
