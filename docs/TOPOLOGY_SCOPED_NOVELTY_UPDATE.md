# Topology-scoped novelty and Figure 2 update

## Figure correction

The pipeline schematic was corrected so that the arm-local feedback path passes below the reject node rather than touching it; node positions, typography and dimensions are unchanged.

## What the loaded library supports

The authoritative screen compares candidates with the loaded MOLE/Corbino--Castillo references under:

1. corner-block normalization invariant to common scale, simultaneous sign, and reflection; and
2. a bounded fixed-length singular-value/spectral fingerprint for unequal closure widths.

For the screened order-six candidates, no direct match occurs. The smallest same-order normalized corner-block distance is `0.6079964839325207`; the compared blocks have unequal shapes. No same-order candidate--reference fingerprint near-match occurs. These are descriptive distances, not standardized novelty statistics.

## Topology scope

Direct coefficient equivalence is defined only for a common target order and a common staggered degree-of-freedom placement. The Corbino--Castillo comparison is now closed by the pinned MOLE source. Parameterized or compact higher-order Castillo--Grone extensions and generalized or block-norm staggered families remain outstanding direct-comparison targets. Classical nodal SBP operators remain relevant conceptual comparators, but a coefficient-level comparison requires a mathematically specified map to the staggered scalar/face spaces.

Consequently, the released objects are described as **exact LLM-originated candidates**, not as previously unknown operator families. `partial_novelty_gate.json` remains fail-closed.
